var assert = require('assert');

describe('TestRunner', function() {
	describe('#constructor(ModuleInfo info)', function() {
		it('should prepare a Karma config to run tests of the module', function() {
			assert(instance.path = info.path);
		});
	});

	describe('#run()', function() {
		it('should run Karma', function() {

		});
	});
});
